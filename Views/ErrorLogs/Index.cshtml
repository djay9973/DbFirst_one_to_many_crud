@model IEnumerable<DbFirst_one_to_many_crud.Models.ErrorLog>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LoggedOn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StackTrace)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LoggedOn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StackTrace)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ErrorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ErrorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ErrorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
