@model DbFirst_one_to_many_crud.Models.Employee
@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@

    <td>
        @Html.DisplayFor(modelItem => Model.FirstName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.LastName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Email)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Mobile)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Description)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Department)
    </td>
    <td>
        <div class="btn-group">
            @*<form asp-action="AddOrEdit"
              data-ajax="true" data-ajax-mode="replace"
          data-ajax-update="#DivListView">
            <button type="submit" class="btn btn-sm btn-primary">
                Add New
            </button>
        </form>*@
            <form asp-action="AddEmployees" data-ajax="true" data-ajax-mode="replace"
                  data-ajax-update="#DivListView">
                <button type="submit" class="btn btn-sm btn-dark">
                    Add New
                </button>
            </form>
                <form asp-action="EditEmployee" asp-route-id="@Model.Id"
                      data-ajax="true" data-ajax-update="#DivDetailView" data-ajax-success="ShowDetailForm()">
                    <button type="submit" class="btn btn-sm btn-dark">
                        Edit
                    </button>
                </form>
                <form asp-action="ViewEmployee" asp-route-id="@Model.Id"
                      data-ajax="true" data-ajax-update="#DivDetailView" data-ajax-success="ShowDetailForm()">
                    <button type="submit" class="btn btn-sm btn-primary">
                        Details
                    </button>
                </form>
                <form asp-action="DeleteEmployee" asp-route-id="@Model.Id"
                      data-ajax="true" data-ajax-success="deleteEmployee(this)">
                    <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Delete Confirmation for Employee @Model.Id');">
                        Delete
                    </button>
                </form>
        </div>
    </td>

